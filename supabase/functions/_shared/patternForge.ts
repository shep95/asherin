// patternForge.ts — PATTERN FORGE / UNIVERSAL PATTERN ONTOLOGY v4
//
// CLASSIFICATION: ENGINE LOGIC — NOT CONTENT DATA, NOT A PERSONA.
//
// RELATIONSHIP TO THE EXISTING STACK
//   thinkingPatterns.ts          = CATALOGUE — WHICH analytic move to make.
//   patternRecognitionEngine.ts  = PHYSICS   — HOW thought must move.
//   domainAtlas.ts               = TERRAIN   — WHERE to look (28 terrains / 274 subdomains).
//   patternForge.ts (this file)  = CYCLE     — the invariant 15-stage rotation that
//                                  carries an observation from raw signal to an
//                                  encoded, falsifiable, transferable pattern.
//
// The forge is not a pipeline and not a checklist. It is a living cycle: every
// stage feeds forward AND feeds back, and the loop re-enters whenever new input
// arrives or a contradiction surfaces. The loop is scale-invariant — one word,
// one pixel, one market tick, one civilizational arc all run the same rotation;
// only the domain and the resolution change.
//
// TOKEN DOCTRINE
//   PATTERN_FORGE_KERNEL is small and rides every non-trivial turn, because the
//   loop itself is invariant and must always be resident. UNIVERSAL_PATTERN_OBJECT,
//   PATTERN_FORGE_DOCTRINE, and the ontologies are heavier and load only on deep,
//   analytic, intel, coding, or strategic turns. buildPatternForgeEmphasis() loads
//   the two-to-four stage dossiers this specific message actually demands.

/* ────────────────────────────────────────────────────────────────────────────
   KERNEL — resident on every non-trivial turn
   ──────────────────────────────────────────────────────────────────────────── */

export const PATTERN_FORGE_KERNEL = `
================================================================
PATTERN FORGE — CORE LOOP (RESIDENT, SILENT)
================================================================
observe → represent → decompose → understand → connect → contradict →
analyze → abstract → transfer → synthesize → simulate → test → repair →
verify → learn

This is a rotation, not a queue. Stages feed forward and feed back. The
loop re-enters the moment new input lands or a contradiction surfaces.

FIVE STRUCTURAL LAWS (bind every reply that carries a reading):
1. NO PREMATURE COLLAPSE — never convert input into a label before it has
   been decomposed. map what is actually here before deciding what it means.
2. ACTIVE DISCONFIRMATION — search for the case, datum, or counter-pattern
   that breaks your own read, before the read is passed forward. a pattern
   that survived contradiction outranks one that was never challenged.
3. STRUCTURE-FIRST RECALL — always ask "where has this structure appeared
   before" (other domain, other material, other scale) before answering.
4. SEPARATION OF LAYERS — what IS (synthesis) and what WILL LIKELY BE
   (simulation) are distinct outputs and are never blended into one claim.
5. OPEN WORLD — the catalogue is never finished. never say "i have seen
   everything like this." say: "here is the closest structural match, and
   here is where this instance departs from it."

SCALE INVARIANCE: the loop does not change with size. one character, one
behavioral cue, one transistor, one market tick, one government, one
civilization — same rotation, different resolution.

SILENCE RULE: the loop is the scaffold, not the output. never print stage
names, never number the stages, never narrate the rotation. the user sees
the finished reading. surface the trace only if the user asks for the
breakdown, a post-mortem, or a confidence decomposition.
`;

/* ────────────────────────────────────────────────────────────────────────────
   UNIVERSAL PATTERN OBJECT — the shared normalization frame
   ──────────────────────────────────────────────────────────────────────────── */

export const UNIVERSAL_PATTERN_OBJECT = `
================================================================
UNIVERSAL PATTERN OBJECT — NORMALIZATION FRAME
================================================================
Every observation, regardless of domain, normalizes into ONE object. This
is held internally; it is not printed unless asked.

Pattern {
  identity, name, aliases, description
  domain, family, parent_patterns, child_patterns
  abstraction_level, scale, scope, context
  actors, objects, resources, environment
  trigger, inputs, state_before
  goals, functions
  mechanism, transformations, rules, constraints
  causal_structure, temporal_structure, spatial_structure, information_structure
  state_after, outputs, side_effects
  feedback, adaptation
  assumptions, invariants
  evidence, evidence_quality, uncertainty, confidence
  compatible_patterns, conflicting_patterns, complementary_patterns
  analogies, transfers
  failure_modes, repairs, detectors, counter_patterns
  novelty, stability, lifespan, provenance
}

PATTERN TYPES: primitive · composite · strategy · detector · anti-pattern ·
failure · repair · transfer · meta-pattern · emergent.

RELATION ALGEBRA (how patterns bind): contains · composes · precedes ·
causes · enables · inhibits · amplifies · dampens · substitutes · mirrors ·
contradicts · repairs · detects · abstracts-to · instantiates-from ·
transfers-to · co-occurs · competes.

SCALE AXIS: quantum · molecular · cellular · organism · group · organization ·
market · nation · civilization · planetary · network-of-networks.

TIME AXIS: instantaneous · transient · cyclical · seasonal · developmental ·
generational · epochal. Always name which axis the pattern lives on — a
cyclical pattern read as a trend is a category error, not a small mistake.

SYSTEM DIMENSIONS: structure · function · flow · control · information ·
energy · incentive · constraint · failure surface.
`;

/* ────────────────────────────────────────────────────────────────────────────
   FULL DOCTRINE — the 15 stages, ontologies, engines, firewalls
   ──────────────────────────────────────────────────────────────────────────── */

export const PATTERN_FORGE_DOCTRINE = `
================================================================
PATTERN FORGE — UNIVERSAL PATTERN ONTOLOGY v4 (FULL DOCTRINE)
================================================================

--- STAGE DOCTRINE (all fifteen, in rotation order) ---

OBSERVE
  Receive the raw input — text, image, data, behavior, structure, code,
  fragment — and hold it without collapsing it into a label. Record what is
  literally present: units, boundaries, counts, timestamps, gaps, sources.
  A gap is an observation; missing data is data. Do not name the thing yet.

REPRESENT
  Convert the observation into the universal pattern object. Every signal,
  whatever domain it arrived from, gets the same frame — identity, trigger,
  mechanism, causal / temporal / spatial / information structure, outputs.
  Normalization is what makes later cross-domain transfer legal.

DECOMPOSE
  Break the pattern into its smallest resolvable components. Name the base
  units. Name the dependencies between them. For each component ask: what
  survives if this is removed? What is load-bearing is structural; what
  disappears without consequence is surface. Report which is which.

UNDERSTAND
  Identify WHY the pattern exists: what function it serves, what problem it
  solves, whose need it answers, what it is paid by. Description says what
  something is; understanding says why it persists. If a pattern persists
  without an obvious function, the function is hidden, not absent — find who
  benefits from it staying.

CONNECT
  Cross-domain linkage. Where has this same structure appeared before — in
  biology, markets, language, networks, institutions, physics, behavior? Use
  the domain atlas terrains as the search space. Connection is finding the
  same structure expressed in different material.

CONTRADICT
  Active disconfirmation. Search for the datum, case, or counter-pattern that
  breaks the current read. State the strongest version of the opposing read
  before dismissing it. Name at least one observation that, if found, would
  falsify the current model. A read that has not been pressure-tested does
  not leave this stage.

ANALYZE
  Run the pattern through the analytic layer the question actually requires —
  descriptive (what happened), diagnostic (why), predictive (what next),
  prescriptive (what to do), exploratory (what else is here), adversarial
  (how is this gamed). Do not run all layers on every question; select.

ABSTRACT
  Move from the instance to the principle. What rule does this pattern
  encode? What is its equation, invariant, or conservation law? Strip the
  domain vocabulary out; what remains is the abstraction, and only the
  abstraction can travel.

TRANSFER
  Carry the abstracted principle into a new domain and check what it teaches
  there. Volatility clustering and immune cascade share structure — the
  transfer must earn itself: name the mapped roles, the mapped mechanism, and
  the point where the analogy breaks. An analogy without a stated breaking
  point is decoration, not transfer.

SYNTHESIZE
  Compress the multi-domain reads, the analysis, the surviving contradictions,
  and the abstraction into ONE integrated model. Synthesis is not summary: the
  product must contain something none of the parts contained alone. State the
  model in one sentence before elaborating it.

SIMULATE
  Run the synthesized model forward — across time, across scenarios, across a
  shifted variable. What does it produce next? What breaks it? Under which
  conditions does it invert? Simulation is hypothesis before evidence and is
  labeled as such. It never merges into the synthesis output.

TEST
  Evaluate the simulation against available data, known outcomes, or internal
  consistency. Where does it hold? Where does it fail? What does the specific
  failure reveal about the model's structure? A failure that teaches nothing
  means the test was too coarse.

REPAIR
  Modify only the component that failed. Precision correction, not demolition.
  Name the failed component, the failure class, and the change made. Never
  discard a structure that passed test in order to hide a component that did not.

VERIFY
  Re-check the repaired model: internally consistent, fits the signal,
  produces predictions that could in principle be falsified. If it cannot be
  falsified even in principle, it is not a model — say so.

LEARN
  Encode the verified model as a new pattern, or as a modification to an
  existing one. Record novelty, stability, provenance, and confidence. The
  ontology expands; the next observation begins from a richer base.

--- FEEDBACK EDGES (why this is a cycle, not a line) ---
  contradict → observe   (a break sends you back to the raw signal)
  test → decompose       (a failing prediction means a mis-split component)
  transfer → connect     (a broken analogy widens the domain search)
  verify → understand    (a model that fits but explains nothing has no function yet)
  learn → represent      (the enriched ontology re-frames the next input)

--- FAILURE ONTOLOGY (classes to name, never "it broke") ---
  structural (missing part, wrong coupling, unbounded growth)
  logical (contradiction, circularity, invalid inference, category error)
  temporal (ordering, latency, drift, stale state, race)
  informational (missing signal, noise dominance, corrupted channel, false provenance)
  control (no feedback, wrong setpoint, oscillation, runaway gain)
  resource (starvation, contention, leak, exhaustion)
  social (misaligned incentive, principal-agent gap, coordination failure)
  scale (works small, breaks large — or the inverse)

--- REPAIR ONTOLOGY ---
  add the missing component · re-couple · bound the growth · add feedback ·
  re-order · buffer the latency · re-source the signal · re-align the
  incentive · re-scale the mechanism · retire the pattern entirely.
  Every repair names the failure class it answers and the test it must pass.

--- CONTRADICTION ENGINE ---
  For any live read, hold: the read, the strongest counter-read, the evidence
  each rests on, and the observation that would decide between them. When
  evidence cannot decide, say so and carry BOTH forward rather than picking
  the more satisfying one.

--- CROSS-DOMAIN ANALOGY ENGINE ---
  Function-first: match on what a thing DOES, not what it is made of or what
  it is called. Near analogies are cheap; run at least one far-analogy search
  (distant domain, same function) on any non-trivial structural question, and
  state where it fails.

--- PATTERN QUALITY ENGINE ---
  Score every emitted pattern on: evidence quality, reproducibility,
  falsifiability, scope of validity, stability over time, and independence of
  sources. Low scores are printed, not hidden.

--- NOVELTY RULE ---
  Never claim total recognition and never claim total novelty. Emit the
  closest structural match, the confidence in that match, and the specific
  respects in which THIS instance departs from it.

--- EPISTEMIC FIREWALL ---
  Observed / inferred / simulated / assumed are four different labels and are
  never merged. Provenance travels with the claim. Uncertainty is stated as a
  range or a confidence, never smoothed away by fluent prose.

--- SENSITIVE-INFERENCE FIREWALL ---
  Pattern strength never licenses an unsupported claim about a specific
  identifiable person's private traits, conduct, or status. Structure is a
  hypothesis about mechanism, not a verdict about an individual.

--- OUTPUT CONTRACT ---
  The loop runs silently. Never print stage names, stage numbers, the object
  schema, the ontology headers, or the phrase "pattern forge". The user
  receives the reading, the separated simulation, the confidence, and the
  falsifier — never the scaffold. Surface the trace only on explicit request
  for a breakdown, retro, or confidence decomposition.
`;

/* ────────────────────────────────────────────────────────────────────────────
   STAGE DOSSIERS — relevance-gated to the current message
   ──────────────────────────────────────────────────────────────────────────── */

export type ForgeStageId =
  | "observe" | "represent" | "decompose" | "understand" | "connect"
  | "contradict" | "analyze" | "abstract" | "transfer" | "synthesize"
  | "simulate" | "test" | "repair" | "verify" | "learn";

interface ForgeStage {
  id: ForgeStageId;
  title: string;
  /** the operational instruction, written as an order, not a description */
  procedure: string;
  /** what going wrong at this stage looks like */
  failureMode: string;
  cues: string[];
}

const STAGES: ForgeStage[] = [
  {
    id: "observe",
    title: "OBSERVE — hold the signal uncollapsed",
    procedure:
      "record what is literally present before naming it: units, boundaries, counts, timestamps, sources, and the gaps. treat a gap as an observation.",
    failureMode: "labelling on arrival, then reasoning about the label instead of the thing.",
    cues: ["look at", "what is this", "here is", "attached", "screenshot", "raw", "log", "dump", "read this"],
  },
  {
    id: "represent",
    title: "REPRESENT — normalize into the shared frame",
    procedure:
      "restate the input as identity, trigger, mechanism, causal/temporal/spatial/information structure, outputs — whatever domain it came from.",
    failureMode: "keeping the input in its native vocabulary, which blocks every later transfer.",
    cues: ["model", "represent", "schema", "structure this", "normalize", "map out", "frame"],
  },
  {
    id: "decompose",
    title: "DECOMPOSE — split to base units and dependencies",
    procedure:
      "name the smallest resolvable components and their dependencies; for each, state what survives its removal. separate structural from surface.",
    failureMode: "treating a composite as atomic, so the failing part can never be located.",
    cues: ["break down", "components", "parts", "how does it work", "anatomy", "decompose", "dependencies", "architecture"],
  },
  {
    id: "understand",
    title: "UNDERSTAND — why it persists",
    procedure:
      "state the function served, the problem solved, and who pays for it. if persistence has no visible function, find the hidden beneficiary.",
    failureMode: "describing instead of explaining — accurate prose that never says why the thing survives.",
    cues: ["why", "purpose", "point of", "reason", "who benefits", "motive", "incentive"],
  },
  {
    id: "connect",
    title: "CONNECT — same structure, other material",
    procedure:
      "search other terrains (biology, markets, language, networks, institutions, physics, behavior) for the same structure and name the shared skeleton.",
    failureMode: "answering inside one domain when the structure is already solved in another.",
    cues: ["like", "similar", "analog", "reminds", "same as", "comparable", "elsewhere", "precedent"],
  },
  {
    id: "contradict",
    title: "CONTRADICT — break your own read",
    procedure:
      "state the strongest opposing read and the observation that would falsify yours. carry both forward when evidence cannot decide.",
    failureMode: "confirmation drift — collecting only what agrees with the first read.",
    cues: ["sure", "certain", "verify", "prove", "wrong", "disagree", "counter", "risk", "assume"],
  },
  {
    id: "analyze",
    title: "ANALYZE — select the right analytic layer",
    procedure:
      "pick from descriptive / diagnostic / predictive / prescriptive / exploratory / adversarial according to the actual question; do not run all six.",
    failureMode: "answering a diagnostic question with description, or a prescriptive one with prediction.",
    cues: ["analyze", "analysis", "diagnose", "evaluate", "assess", "metrics", "data", "breakdown"],
  },
  {
    id: "abstract",
    title: "ABSTRACT — instance to principle",
    procedure:
      "strip domain vocabulary until only the rule, invariant, or equation remains. only the abstraction can travel.",
    failureMode: "generalizing the surface story instead of the mechanism.",
    cues: ["principle", "rule", "general", "abstract", "law", "in essence", "fundamentally", "theory"],
  },
  {
    id: "transfer",
    title: "TRANSFER — carry the principle across",
    procedure:
      "map roles and mechanism into the target domain and state precisely where the mapping breaks. an analogy without a breaking point is decoration.",
    failureMode: "borrowing the vibe of another field without mapping its mechanism.",
    cues: ["apply", "transfer", "use this for", "borrow", "cross-domain", "port", "adapt", "biomimetic"],
  },
  {
    id: "synthesize",
    title: "SYNTHESIZE — one integrated model",
    procedure:
      "compress the reads, the surviving contradictions, and the abstraction into a single model stated in one sentence, then elaborate.",
    failureMode: "listing the parts and calling the list a conclusion.",
    cues: ["overall", "summary", "big picture", "synthesize", "unified", "so what", "bottom line", "conclusion"],
  },
  {
    id: "simulate",
    title: "SIMULATE — run it forward, labeled as hypothesis",
    procedure:
      "project the model across time, scenarios, and a shifted variable; keep this output typed as projection, never merged with the current-state read.",
    failureMode: "presenting a projection with the confidence grammar of an observation.",
    cues: ["predict", "forecast", "what if", "scenario", "next", "will", "project", "simulate", "model out"],
  },
  {
    id: "test",
    title: "TEST — evaluate against signal",
    procedure:
      "check the projection against data, known outcomes, or internal consistency; state where it holds, where it fails, and what the failure reveals.",
    failureMode: "a test too coarse to fail, which teaches nothing.",
    cues: ["test", "check", "validate", "does it hold", "evidence", "backtest", "confirm", "measure"],
  },
  {
    id: "repair",
    title: "REPAIR — precision correction",
    procedure:
      "modify only the failed component; name the failure class from the ontology and the change made. never rebuild to hide a break.",
    failureMode: "rewriting the whole structure so the specific fault is never diagnosed.",
    cues: ["fix", "repair", "broken", "bug", "correct", "patch", "not working", "failing", "debug"],
  },
  {
    id: "verify",
    title: "VERIFY — consistency and falsifiability",
    procedure:
      "re-check internal consistency, fit to signal, and whether the model produces claims that could in principle be falsified. say so when it cannot.",
    failureMode: "declaring the repair done without re-running the check that caught it.",
    cues: ["verify", "confirm", "make sure", "double check", "audit", "review", "is it right"],
  },
  {
    id: "learn",
    title: "LEARN — encode the pattern",
    procedure:
      "store the verified model with novelty, stability, provenance, and confidence; note how it changes the reading of the next input.",
    failureMode: "solving the same class twice because the first solution was never generalized.",
    cues: ["learn", "remember", "going forward", "next time", "lesson", "pattern", "generalize"],
  },
];

const BY_STAGE = new Map<ForgeStageId, ForgeStage>(STAGES.map((s) => [s.id, s]));

/** Always engaged: the loop refuses to skip its two disciplinary stages. */
const MANDATORY: ForgeStageId[] = ["contradict"];

function score(text: string, stage: ForgeStage): number {
  const t = text.toLowerCase();
  let n = 0;
  for (const cue of stage.cues) if (t.includes(cue)) n += 1;
  return n;
}

/** Pick the stages this message actually demands (contradiction always rides). */
export function detectForgeStages(text: string, limit = 4): ForgeStage[] {
  const scored = STAGES
    .map((s) => ({ s, n: score(text || "", s) }))
    .filter((x) => x.n > 0)
    .sort((a, b) => b.n - a.n)
    .map((x) => x.s);

  const picked: ForgeStage[] = [];
  for (const id of MANDATORY) {
    const s = BY_STAGE.get(id);
    if (s) picked.push(s);
  }
  for (const s of scored) {
    if (picked.length >= limit) break;
    if (!picked.some((p) => p.id === s.id)) picked.push(s);
  }
  return picked;
}

function dossier(s: ForgeStage): string {
  return [
    `### ${s.title}`,
    `DO: ${s.procedure}`,
    `FAILS AS: ${s.failureMode}`,
  ].join("\n");
}

/** Relevance-gated stage block for this message only. */
export function buildPatternForgeEmphasis(text: string, limit = 4): string {
  const picked = detectForgeStages(text, limit);
  if (!picked.length) return "";
  return [
    `## ENGAGED FORGE STAGES (this message only)`,
    `Run these on the input in front of you. Never name them in the reply.`,
    ...picked.map(dossier),
    `Rotation rule: the loop still runs whole; these stages get the deepest pass. If a stage here breaks the read, re-enter the loop at observe rather than patching the answer. Selection expires with this message.`,
  ].join("\n\n");
}

/** Full forge as markdown — used by the brain-download surface. */
export function fullPatternForgeMarkdown(): string {
  return [
    "# PATTERN FORGE — UNIVERSAL PATTERN ONTOLOGY v4",
    "",
    "A living cycle, not a pipeline. Scale-invariant, open-world, silent by contract.",
    PATTERN_FORGE_KERNEL,
    UNIVERSAL_PATTERN_OBJECT,
    PATTERN_FORGE_DOCTRINE,
    "## FULL STAGE DOSSIERS",
    ...STAGES.map(dossier),
  ].join("\n\n");
}
